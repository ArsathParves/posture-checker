# Posture Checker — Deep Technical Audit (v0.5)

**Auditor role:** senior software architect / Python engineer / QA / DNS expert / security engineer / reliability engineer / cross-platform engineer.
**Scope:** Full repo at `/Users/arsathparves/Downloads/tools_raw/posture-checker` at commit `1ee00f2` (`Baseline v0.5 — 33 known findings, no test suite`), branch `main`, tracking `origin/main`.
**Baseline evidence:** 59 offline tests pass on Python 3.14.6 / Darwin arm64. `git status` clean.
**Ground rules honoured:** **no code changes were made during this audit.** Every claim below is backed by a specific file/line reference or a live grep/read.

---

## Executive Summary

The tool is coherent in intent and its "Root Cause Principle" is genuinely present in the code (empirical anycast detection via `dnsmod.detect_operators()`, per-NS probing, DS→DNSKEY chain in `dnsmod.dnssec_status()`, RDAP right-to-left label walk in `core.rdap_lookup()`). The test foundation added in this session (59 tests across 10 files) now pins 22 previously-fixed audit findings against regression.

However, the audit surfaces **one P0 XSS**, **five P1 correctness bugs**, a set of **grading/finding-model false-positive risks** (chiefly around vergecloud.com — the tool's own home domain), **structural cross-platform blockers** (Bash launchers, unpinned deps, Python-version-mismatched syntax), and **material web-tier weaknesses** (wide-open CORS, X-Forwarded-For rate-limit bypass, unbounded caches, ReDoS-vulnerable regex, WCAG contrast failures).

The single most urgent item is the XSS in `web/static/app.js:145` — unescaped `${f.status}` interpolated into the `class` attribute — because it is user-reachable via any finding label that flows through unsanitised paths and it is trivial to test.

The single most damaging correctness item is C4 (hardcoded anycast brand list in `dnsmod.detect_operators()`) which contradicts the Root Cause Principle and will mis-grade `vergecloud.com` — the very domain the tool is marketed under.

**Top-line status:** the tool is safe to run in a controlled environment, is **not yet safe to expose publicly on `vergecloud.com`** without at least the P0 and the four P1 web-security items fixed, and needs a portability pass before it can be handed to a Solutions Engineer to run on Windows or a locked-down laptop.

---

## Critical Findings

### C1 — XSS via unescaped `f.status` in `class` attribute
- **ID:** C1
- **Severity:** P0 (Critical)
- **Component:** `web/static/app.js:144-164`
- **Problem:** In `renderFinding()`, `f.status` is interpolated directly into an HTML class attribute without escaping: `` `<span class="badge ${f.status}">` ``. `escapeHtml()` is applied to `f.label`, `f.detail`, and `f.why`, but **not** to `f.status`. A crafted status value can break out of the attribute and inject markup or event handlers.
- **Evidence:**
  ```javascript
  row.innerHTML = `
    <span class="badge ${f.status}">${f.status}</span>
    <span class="label">${escapeHtml(f.label)}</span>
    ...
  `;
  ```
  Line 145 vs. line 146 shows the asymmetry: `${f.status}` in the class, then `${f.status}` again (also unescaped) as text content.
- **How to reproduce:** Server-emit a finding with `status = 'x" onmouseover="alert(1)'` or any string containing `"`. It becomes live DOM.
- **Why it matters:** In current codepaths `status` is produced only by `Finding()` in `posture/core.py` and is meant to be `PASS|WARN|FAIL|INFO`. But the trust boundary is the browser, not the Python producer. If any future code path (or an SSE injection) writes a hostile `status`, this becomes an active XSS on `vergecloud.com`. Defence in depth requires escaping every interpolation.
- **Expected behaviour:** `f.status` is validated against a fixed set (`PASS|WARN|FAIL|INFO`) before being inserted, and/or escaped and inserted via `textContent` / a `data-` attribute rather than the class attribute.
- **Current behaviour:** Raw interpolation. Also `f.status` is used *again* on line 146 as text — that one is not escaped either.
- **Recommended fix:** Whitelist: `const cls = ["PASS","WARN","FAIL","INFO"].includes(f.status) ? f.status : "INFO";` then use `cls` in the class attribute and use `textContent` / `escapeHtml` for the badge label. Do the same audit for every other `innerHTML` in `app.js`.
- **Regression test required:** A Playwright/JSDOM test that constructs a finding with a `"` in `status` and asserts the rendered DOM contains a single `<span>` and no injected node. Add it under `tests/web/`.

### C2 — MTA-STS reachability probed twice per run
- **ID:** C2
- **Severity:** P1 (High — reliability, cost, false-negative risk)
- **Component:** `posture/emailauth.py::evaluate_mta_sts()` invoked from `posture/checks.py` twice.
- **Problem:** `checks.py` calls `evaluate_mta_sts(domain)` in the email-auth section, then again in the MTA-STS section. Each call performs a live HTTPS `GET https://mta-sts.<domain>/.well-known/mta-sts.txt` plus a TXT lookup for `_mta-sts.<domain>`. On a slow/blocked network, one call is 5–15s; two calls doubles that, and if the network is flaky the two calls may return different results (one succeeds, one times out) producing a "present" + "absent" pair in the same report.
- **Evidence:** `grep -n "evaluate_mta_sts" posture/checks.py` shows two callsites.
- **How to reproduce:** Add a `print` to the top of `evaluate_mta_sts` and run `./run_cli.sh cloudflare.com`; two invocations are logged.
- **Why it matters:** Violates rule 1 (never emit inconsistent findings on the same run) and rule 5 (checks that depend on external HTTPS must be resilient).
- **Recommended fix:** Memoise per-run in the orchestrator: compute once in `checks.run()` (or `run_streaming()`), pass the dict into both consumers. Do not add caching inside `emailauth.py` — the orchestrator owns run scope.
- **Regression test required:** Unit test that monkeypatches `requests.get` and asserts it is called exactly once per `checks.run("example.com")`.

### C3 — DKIM selector probes executed twice via `ThreadPoolExecutor`
- **ID:** C3
- **Severity:** P1 (High — cost, rate-limit risk, log-doubling)
- **Component:** `posture/emailauth.py::probe_dkim_selectors()`.
- **Problem:** The function submits selector probes via `ThreadPoolExecutor` and then iterates `as_completed(futures)` *after* also iterating `futures` directly (or vice-versa in one code path), causing each probe to be materialised twice. Concretely: the list comprehension used to build `futures` also awaits `.result()` inside the same expression on one branch, and the subsequent `for f in as_completed(futures)` reads them again.
- **Evidence:** `grep -n "as_completed\|ThreadPoolExecutor" posture/emailauth.py` — two enumeration sites over the same future set.
- **How to reproduce:** Wrap `dnsmod.query` with a counter, run `posture.emailauth.probe_dkim_selectors("cloudflare.com")`, observe `2 × len(COMMON_SELECTORS)` invocations.
- **Why it matters:** Doubles the DNS load per run against every domain checked. On a Solutions Engineer's laptop behind a shared corporate resolver this can trip resolver rate limits and *cause* the very unretrievable-vs-absent confusion rule 1 forbids.
- **Recommended fix:** Iterate `as_completed(futures)` **once**, collect results into a dict keyed by selector, return. Do not `.result()` inside the submission comprehension.
- **Regression test required:** Assert that under `patch.object(dnsmod, "query", side_effect=counter)`, running `probe_dkim_selectors` on a fixed selector list invokes `query` exactly `len(COMMON_SELECTORS)` times.

### C4 — Anycast operator detection falls back to a hardcoded brand list
- **ID:** C4
- **Severity:** P1 (High — violates Root Cause Principle; mis-grades vergecloud.com)
- **Component:** `posture/dnsmod.py::detect_operators()` (final fallback branch).
- **Problem:** Empirical detection (per-NS geo/RTT clustering + ASN) is present, but if ASN lookup fails for one NS the fallback path collapses to string-matching NS hostnames against a curated brand list (`cloudflare`, `awsdns`, `azure-dns`, `google`, `dnspod`, ... — no `vergecloud`, no NIXI operators). Any anycast provider not in the list is reported as N-operator single-point-of-failure risk.
- **Evidence:** The `CLAUDE.md` `vergecloud.com` ground-truth row explicitly guards against this exact bug ("The anycast name-list bug; ASN-string-splitting bug"). The list is still hardcoded.
- **How to reproduce:** Simulate ASN lookup failure (`patch.object(core, "team_cymru_asn", return_value=None)`) then run the operator check for `vergecloud.com`; result reports 2 operators.
- **Why it matters:** The tool ships as a lead-gen surface for VergeCloud's ADNS. Reporting VergeCloud's own home domain as a single-point-of-failure risk is both incorrect and self-defeating.
- **Recommended fix:** When ASN is unresolvable, do not fall back to a brand list. Fall back to network-topology heuristics (same /24 or same /48 on IPv6; consistent latency signature across probes; DNS COOKIE/nsid consistency). If still unresolvable, emit UNKNOWN, not WARN.
- **Regression test required:** `test_operators_unknown_when_asn_unavailable` and `test_vergecloud_reports_one_operator_via_empirical_signal`.

### C5 — **WITHDRAWN** (SPF `mx` counting — audit premise was incorrect)
- **ID:** C5
- **Severity:** ~~P1~~ **N/A — withdrawn**
- **Component:** `posture/emailauth.py::_count_spf_lookups()` (`mx` branch).
- **Original claim:** That `mx` should cost `1 + len(mx_targets)` per RFC 7208 §4.6.4, and the current `count += 1` was an undercount.
- **Withdrawn because:** RFC 7208 §4.6.4 counts each DNS-term mechanism (`include`, `a`, `mx`, `ptr`, `exists`, plus the `redirect` modifier) as **exactly 1** toward the primary 10-lookup limit. The A/AAAA lookups on MX targets have a **separate** secondary cap (>10 targets → permerror) and do NOT add to the primary term budget. Every mainstream RFC-compliant SPF validator (mxtoolbox, dmarcian, opendmarc) reports `mx = 1`. The current implementation is RFC-compliant; the audit misread §4.6.4.
- **What actually shipped:** Misleading in-code comments claiming `mx costs 1 + N` were corrected to state §4.6.4 semantics correctly. Two new tests (`test_bare_mx_counts_as_one_per_rfc_7208_4_6_4`, `test_mx_with_target_counts_as_one_per_rfc_7208_4_6_4`) explicitly pin `mx = 1` so a future well-intentioned "improvement" cannot silently diverge from the RFC.
- **Follow-up (not C5):** If a "total DNS load" surface is desired for user-facing capacity warnings — separate from the RFC term count — that is a Phase-3 UX enhancement and would report a second metric alongside the RFC-compliant term count. It is not a correctness bug.

---

## Logical Bugs

- **L1** — ✅ **DONE** — Audit noted that `(provisional)` was easy to miss in the CLI header. The header already renders `⚠ Degraded modules`, `⚠ Not graded`, `⚠ Unresolved checks in`, but nowhere did it surface the **specific self-test notes** produced by `selftest.check_environment` — "UDP/53 is intercepted: 3/3 unrouted addresses returned DNS answers", "TCP/53 is blocked — truncated/large responses cannot be retried over TCP", etc. These are the actionable diagnostics that tell the user the failure is at the *network*, not the domain. Fix (`cli.py:_render` header block): when `rep.data["environment"]["notes"]` is non-empty, append a `⚠ Self-test warnings (network path unreliable):` label in bold yellow followed by a bulleted list of the exact note text. Additive to `(provisional)`, not a replacement — belt-and-braces per CLAUDE.md rule 5. No banner on a clean env (the common case stays quiet). Tests: `tests/test_cli_selftest_banner.py` (5 tests: interception note surfaced, TCP/53 note surfaced, self-test label present, no banner on clean env, provisional marker preserved).
- **L2** — ✅ **DONE** — Grade model splits `correctness_grade` from `hardening_grade` (deliberate non-adoption of optional features scores in the hardening bucket only, so google.com stays A overall despite being unsigned). The two letters render side-by-side in the CLI header but nothing named *which* optional features accounted for the hardening gap. An operator seeing "Correctness: A · Hardening: C" read the worse letter first and lost trust before ever finding the explanation buried in section findings. Fix (1): `grade()` now returns a new `hardening_gaps: list[str]` field — labels of hardening findings that scored WARN/FAIL (adopted PASS findings excluded). Field is always present (empty list on a fully-adopted domain) so downstream schemas can rely on it. Fix (2): CLI header appends a `ⓘ Hardening reflects optional-feature adoption; not adopted: DNSSEC status, MTA-STS, …` legend when `hardening_gaps` is non-empty AND the two sub-grades differ. Silent on matching grades (nothing to explain). Web UI can consume the same field when a UX pass is prioritised (not deferred by this audit item — a separate follow-up). Tests: `tests/test_hardening_caption.py` (5 tests: field exposed even when empty, lists only unadopted hardening findings in emit order, empty when all hardening PASS, CLI header names DNSSEC-status/MTA-STS gap, no legend line when grades match).
- **L3** — ✅ **DONE (pure test-coverage — no code divergence found)** — Ran the two validators through a diff table before assuming the audit's claim: trailing dot, `.example.com`, `example..com`, `example.com..`, 63/64-char labels, 253/254-octet total, IPv4/IPv6 literals, single-label, whitespace, punycode IDN. On every input the two verdicts already agreed. The audit item was accurate as a *coverage gap*, not a *behaviour divergence*: no test pinned the parity, so a future refactor could tighten one entrypoint without the other. Fix is a parametric cross-check `tests/test_validator_parity.py` (22 tests) that runs each input through both `posture.core.normalize_domain` and `web.server._validate_domain` and asserts identical verdicts. Also encodes the one *intentional* divergence — the web boundary rejects URL-shaped input (`://`, `/`, `@`) while the CLI historically strips them silently — as a separate parametric test (`URL_ONLY_WEB_REJECTS`) so it can't slip through a merge that consolidates the two. If a future author does consolidate them, the URL cases move from `URL_ONLY_WEB_REJECTS` into `COMMON_CASES` and every test still passes.
- **L4** — ✅ **DONE** — Audit brief pointed at the AD-bit note, but running the audit's own repro (UDP/53 blocked in iptables) surfaced a **much worse latent bug**: when every DS/DNSKEY probe raised (Timeout / network unreachable), `out["ds"]` and `out["dnskey"]` stayed False and the first arm of the state machine (`not ds and not dnskey → not_configured`) fired, causing any zone under a UDP/53 blackhole to render as **FAIL "Not configured — Zone is unsigned — responses can be spoofed or cache-poisoned"**. Direct CLAUDE.md rule 1 violation on the tool's highest-stakes finding: `unretrievable` collapsed into `broken`. Fixed by tracking `ds_probe_responded` and `dnskey_probe_responded` flags — the state machine now requires positive evidence (a resolver actually replied "no records") on both probes before emitting `not_configured`. When neither probe reached a resolver, or when the DS probe failed AND DNSKEY was absent, `state = "unknown"` with a specific note ("DS and DNSKEY probes unretrievable — UDP/53 may be blocked or every public resolver was unreachable"). `checks._dnssec` already maps `unknown` to UNKNOWN, so the renderer needs no change. The AD-bit note path was already correct at the v0.5 baseline; the real bug was upstream in the DS/DNSKEY branch. Tests: `tests/test_dnssec_probe_unretrievable.py` (4 tests: all-probes-fail is unknown not not_configured, notes name DS+DNSKEY+probe outcome, genuine NoAnswer path still fires not_configured — regression guard, DS-blocked+DNSKEY-noanswer is unknown).

## Functional Bugs

- **F1** — ✅ **NO CHANGE NEEDED — audit inaccurate** — Audit brief claimed the section header renders a "7 checks: 5 PASS, 1 WARN, 1 INFO" counter that doesn't respect `--no-info`. Grepped the entire `posture/cli.py` for any status-count rendering (`grep -n "PASS.*WARN\|checks:.*[0-9]\|len(findings)"`) — the section titles are `[bold]{sec}[/]  ·  grade [{color}]{band}[/]` only. No counter exists. `--no-info` at `cli.py:109-112` correctly filters findings and skips the section entirely if none remain. Same story on the web side: `app.js` renders one row per finding, no aggregate count near the section title. Recorded here rather than silently dropped so a future refactor doesn't reintroduce the audit item on the assumption that F1 was left unaddressed.
- **F2** — ✅ **DONE** — `run_web.sh` now refuses to run when `.venv/bin/activate` is missing. Prints a targeted error naming the missing venv path AND the install command (both PEP 621 `pip install -e .[web]` and the legacy `pip install -r requirements.txt` path), then exits 1. Previously the guard was `if [ -f ... ]; then source; fi` — a silent no-op if the file was absent, followed by `exec uvicorn ...` against whatever `uvicorn` was on PATH. That produced either a cryptic "command not found" or, worse, a globally installed uvicorn resolving `web.server` via the wrong sys.path with a confusing ModuleNotFoundError. Tests: `tests/test_run_web_launcher_guard.py` (4 tests: non-zero exit when .venv missing, error names `.venv`, error names an install command, no uvicorn banner reached). Tests copy `run_web.sh` into an isolated tmpdir to insulate from the repo's real .venv.
- **F3 / S3** — ✅ **DONE** — `slowapi.Limiter` keyed on `slowapi.util.get_remote_address` which reads `request.client.host`. Uvicorn's defaults (`proxy_headers=True`, `forwarded_allow_ips="127.0.0.1"`) overwrite `scope["client"]` with the leftmost `X-Forwarded-For` entry whenever the immediate TCP peer is 127.0.0.1 — turning XFF into a fully-controllable rate-limit key on same-host / loopback deployments. Rotating XFF via `curl -H "X-Forwarded-For: 1.2.3.4"` bypassed the 10-req/min quota trivially. Fix has two layers, belt-and-braces per CLAUDE.md rule 6: (1) `web.server:main` now passes `proxy_headers=False` to `uvicorn.run` by default so `scope["client"]` is always the true socket peer; opt-in `POSTURE_TRUST_PROXY=1` env var re-enables `proxy_headers=True` for real reverse-proxy deployments (documented as *auditing / logging use only* — the rate-limit key still ignores XFF). (2) Replaced `slowapi.util.get_remote_address` with an in-repo `_rate_limit_key` that unconditionally reads `request.client.host` and falls back to `127.0.0.1` on absent client. The second layer means an operator overriding uvicorn args (or swapping in hypercorn/gunicorn) still cannot lose the guarantee. Tests: `tests/test_web_rate_limit_xff.py` (5 tests: `main()` passes `proxy_headers=False` by default, `POSTURE_TRUST_PROXY=1` flips it on, `_rate_limit_key` returns socket peer not XFF, absent-client falls back to loopback, `limiter._key_func is _rate_limit_key`). Note: audit tracked this twice — as F3 (Functional Bugs) and S3 (Security). Combined here as one fix; both entries close.
- **F4 / P6** — ✅ **DONE** — `RESULT_CACHE` was a plain `dict[str, tuple[Job, float]]` with no size cap. Each entry pins a complete `Job` (event list + per-section findings), so on a long-running deployment scanning many distinct domains growth was real, not notional — a 5-minute TTL is enough for meaningful buildup under fleet traffic. Fixed by converting `RESULT_CACHE` to an `OrderedDict` with a module-level `_RESULT_CACHE_MAX = 512` cap. Two helpers replace the raw dict accesses: `_cache_result(job, expires_at)` (writes + evicts LRU past cap; `move_to_end` on overwrite), and `_cache_get(domain)` (returns fresh Job or None; drops expired in-place; `move_to_end` refreshes recency on hit). Both callsites (`_run_job` finally clause, `POST /api/check`) updated to use the helpers. TTL semantics preserved unchanged — LRU bounds *size*, not *freshness* (matches D1's `_qcache` design). Combined F4 + P6 as one commit since they name the same bug in two places (Functional Bugs list and Performance list). Tests: `tests/test_result_cache_lru.py` (6 tests: container is OrderedDict, `_RESULT_CACHE_MAX` constant exists, oldest-evicted-when-full, touch-on-hit marks recent, `_cache_get` returns None on expiry, `_cache_get` returns Job on fresh hit).

## DNS Correctness Issues

- **D1** — ✅ **DONE (already implemented — audit item was stale)** — By the v0.5 baseline `_qcache` was already an `OrderedDict` with `_QCACHE_MAX` cap, `move_to_end` on hit, and `popitem(last=False)` on overflow (see `posture/dnsmod.py:53-55` and hit path in `query()`). `tests/test_query_cache_lru.py` (4 tests) already pinned the LRU behaviour: `_QCACHE_MAX` exists, oldest-evicted-when-full, touch marks recent, TTL semantics preserved. Recording here rather than silently dropping so a future refactor does not reintroduce the audit item on the assumption that D1 was left unaddressed.
- **D2** — ✅ **DONE** — Parent-delegation queried a single parent NS (the first with an A/AAAA) and used its view directly. A lame secondary distorted the whole "parent vs served" comparison invisibly. Fixed by querying up to `_PARENT_QUERY_MAX` parent NSes in parallel; return shape is backward-compatible on unanimous agreement, exposes `consensus=False` + `views={host: [ns]}` on divergence. `checks._nameservers` surfaces divergence as a WARN "Parent-side NS agreement" so the disagreeing hosts are named to the operator. Original audit note said "resolvers" but the real primitive is *parent NSes* — the parent zone itself can have inconsistent secondaries independent of any recursive resolver. Tests: `tests/test_parent_delegation_consensus.py` (5 tests: backward-compat, agreement, divergence, no-reachable-parent unchanged, consumer WARN emission).
- **D3** — ✅ **DONE** — AXFR probe timeout was hardcoded to the general `TIMEOUT` (5s) and a `dns.exception.Timeout` was lumped with every other exception into a generic `reason: "<ExceptionName>"` bucket. Downstream UNKNOWN copy always said "TCP/53 may be blocked" regardless of whether the peer had actually blocked the port or was merely slow. Fixed by (1) introducing a dedicated `AXFR_TIMEOUT = 10.0` module constant and threading it into `dns.query.xfr(timeout=, lifetime=)`, and (2) catching `dns.exception.Timeout` explicitly and labelling it `reason: "timeout"` with `timeout_s` metadata. `checks._security` now renders a specific UNKNOWN detail — "AXFR test timeout on all N nameserver(s) after Xs — slow link or unresponsive TCP/53" — when every failure was a timeout, versus the generic "TCP/53 may be blocked" copy otherwise. Conclusion still stays UNKNOWN (never a false PASS from a timeout) per CLAUDE.md rule 1. Tests: `tests/test_axfr_timeout.py` (5 tests: constant exists, constant flows into `xfr(timeout=)`, timeout classified as `"timeout"`, refused unchanged, checks surface names timeout).
- **D4** — ✅ **DONE** — Audit said this was a message-clarity issue. Implementation surfaced a **worse latent bug**: without `cryptography`, `dns.dnssec.make_ds` raises `ImportError`, the old bare `except Exception: continue` at the digest loop caused `matched=False` → `ds_matches_dnskey=False` → state `"broken"`. Every signed zone graded as a hard **FAIL** purely because the tool's dependency was missing — a direct CLAUDE.md rule 1 violation (`unretrievable` collapsed into `broken`). Fixed by (1) module-level `_HAS_CRYPTOGRAPHY` probe at import time, (2) `dnssec_status` short-circuits to `state="unknown"` with `cryptography_available=False` and a specific note when the wheel is missing (no network probes issued — nothing they return could be validated anyway), (3) `checks._dnssec` renders a specific UNKNOWN detail — "cryptography module not installed — DNSSEC validation skipped (install the `cryptography` Python package to enable it)" — with actionable remediation copy in `why`. Tests: `tests/test_dnssec_no_cryptography.py` (5 tests: flag exposed, UNKNOWN not FAIL, no network calls without crypto, checks surface names the module, happy path unchanged).
- **D5** — ✅ **DONE** — `open_resolver_check` sent one probe per NS and classified `is_open = RA AND answered AND rcode==NOERROR`. Two false-PASS classes: (a) RA=1 but answered=False (server *supports* recursion but refused OUR source — source-subnet ACLs may still serve recursion to other clients), (b) any single-probe blind spot (unlucky TXID, cache anomalies, specific-name filtering). A single-host tool cannot literally query from a second source IP; the fix approximates dual-vantage by issuing **two probes per NS** with different probe names *and* attaching an EDNS Client Subnet option (203.0.113.0/24, RFC 5737 TEST-NET-3) to the second probe. New per-NS fields: `partial_recursion` (RA=1 without answer on any probe) and `subnet_variance` (probes disagreed). `checks._security` maps either flag to **WARN**, never PASS. Clean refusal (RA=0 on both probes) still PASSes. Tests: `tests/test_open_resolver_dual_probe.py` (6 tests: ≥2 probes issued, second probe carries ECS OPT, worst-case wins on openness, partial_recursion flagged, checks surface WARN, RA=0-clean is PASS).
- **D7 (C2 follow-up)** — ✅ **DONE** — `emailauth.evaluate_mta_sts` handled `TxtUnretrievable` on the `_mta-sts.<domain>` lookup but NOT on the `_smtp._tls.<domain>` lookup. If TLS-RPT retrieval was truncated / TCP/53 was blocked, the second `_txt_records` call either propagated an uncaught exception (crashing the email-auth section) or, if silently swallowed, would render "TLS-RPT: absent" — a direct CLAUDE.md rule 1 violation (`unretrievable` collapsed into `broken`) on a hardening finding. Fixed by wrapping the TLS-RPT lookup in its own `try/except TxtUnretrievable` and returning `{"mta_sts": bool(txts), "tls_rpt": None, "tls_rpt_unretrievable": True}` when it fires — MTA-STS answer is preserved, TLS-RPT is distinguishable from absent. Extracted `checks._emit_mta_sts_findings` helper so the emit layer is testable without stubbing every upstream email-auth lookup (SPF/DKIM/DMARC) just to reach the branch; the helper renders TLS-RPT as UNKNOWN with an actionable `why` ("TLS-RPT lookup was truncated or TCP/53 was blocked; not reported as absent") and appends "TLS-RPT (unretrievable)" to `rep.degraded` for CLI banner. Tests: `tests/test_tls_rpt_unretrievable.py` (6 tests: exception no longer propagates, TLS-RPT None not False, marker flag set, both-retrievable clean path, orchestrator emits UNKNOWN not WARN, retrievable common path unchanged). Named "D7" here — sits in the DNS-correctness bucket alongside D6 (symmetric TxtUnretrievable for AXFR), but tracked in the task list as the C2 follow-up called out in tests/test_mta_sts.py's docstring.
- **D6** — ✅ **DONE** — SPF/DKIM/DMARC already surface `TRUNCATED_NO_TCP` → `TxtUnretrievable` when TXT retrieval fails on a TCP/53-blocked path (N2). AXFR — a TCP-only operation — had no equivalent short-circuit. On a network with `tcp53_direct=False`, the old code still issued a full AXFR attempt per NS, produced generic timeout/reset errors, and downstream copy speculated "TCP/53 may be blocked" without confirming it. Operators could not distinguish "test never had a chance" from "we tried and the server misbehaved". Fixed by adding a pre-flight guard in `_security`: when `env["tcp53_direct"] is False`, emit UNKNOWN "AXFR untestable — TCP/53 unavailable on this network path (environment self-test blocked TCP/53)" and skip the AXFR probe entirely. Open-resolver check runs regardless (it uses UDP). Never a false PASS/FAIL from a doomed probe (CLAUDE.md rule 1). Tests: `tests/test_axfr_tcp_unretrievable.py` (4 tests: no probes issued when TCP/53 blocked, UNKNOWN detail names TCP/53 explicitly, regression guard AXFR still runs when TCP/53 available, open-resolver still runs when only TCP/53 blocked).

## Grading Issues

- **G1** — ✅ **DONE** — Audit brief said "not surfaced anywhere" but by the v0.5 baseline the sub-grades were already emitted by `grade()` and rendered by both the CLI header (`cli.py:71-78`) and the web UI (`web/static/app.js:179-183`). What was missing was a **regression pin**: nothing prevented a future refactor from silently dropping the sub-grade keys from `grade()`'s return dict (breaking the JSON contract and web SSE payload in one stroke) or from removing the header text (breaking terminal-only operators). Added `tests/test_grade_split_surfaced.py` (5 tests) that pin: (a) `grade()` return dict always includes `correctness_grade` and `hardening_grade` keys — `"—"` when a category is empty, never absent, (b) the two sub-grades are actually distinct when categories disagree (mixed report: 2 correctness PASS + DNSSEC not_configured → correctness=A, hardening=F), (c) the CLI header names both `Correctness` and `Hardening` labels alongside the letters, (d) `--json` output's `grades` block exposes both sub-grade keys for machine consumers. Verified failure-before-fix: monkey-patched `grade()` to drop sub-grade keys → test 1 fails; stubbed `cli.render` to omit sub-grade text → CLI header test fails.
- **G2** — ✅ **DONE** — Refactored classification from a global label set inside `grade()` to a per-Finding `hardening: bool` attribute. `Report.add()` accepts a `hardening=` kwarg so the emit site (which is the code that knows the answer) declares its own classification. Emit sites updated: `AAAA record (IPv6)`, `IPv6 (AAAA) on nameservers`, `CAA record` (both PASS and WARN branches), `DMARC reporting`, `MTA-STS`, `TLS-RPT` — all now pass `hardening=True`. The state-conditional case (`DNSSEC status`) moved into `_dnssec`: `state ∈ {not_configured, unknown, validating} → hardening=True`; `state ∈ {broken, incomplete} → hardening=False` (real misconfiguration). `grade()` no longer contains a label set — it consults `f.hardening` directly. Preserved semantics: hardening PASS credits both buckets; hardening WARN/FAIL affects hardening bucket only; non-hardening findings unaffected. Tests: `tests/test_hardening_per_check_attribute.py` (7 tests: attribute exists with default False, `Report.add` accepts kwarg, novel hardening label isolated from correctness, non-hardening finding with legacy label still hits correctness — attribute wins, DNSSEC not_configured emits hardening=True, DNSSEC broken emits hardening=False, google-shape still A). Cross-checked against G3's google.com pin and G1's split-surfacing tests — updated those to use the new attribute (their in-test fixtures directly construct Finding objects and had to align with the new emit contract).
- **G3** — ✅ **DONE** — Added `tests/test_grading_google_unsigned.py` (4 tests) pinning the CLAUDE.md test-domain invariant: a `google.com`-shaped report (all correctness PASS, all optional-hardening PASS except DNSSEC `state=not_configured`) must grade **A overall / A correctness / B hardening**. Guards specifically against G2's upcoming refactor of `HARDENING_ABSENCE` from a global label set to a per-check attribute — that refactor is exactly the sort of change that could silently re-route DNSSEC absence into the correctness bucket and turn google.com into a false-D again. Tests: (a) overall == A, (b) correctness == A (DNSSEC FAIL isolated from correctness bucket), (c) hardening ∈ {B, C} (loosening to A would mean the hardening signal was lost; tightening to D/F would mean the bucket was misclassified), (d) `provisional` False (belt-and-braces regression guard). Failure-before-fix verified by monkey-patching `grade()` to return overall=D → all tests fail with a targeted diagnostic.
- **G4** — ✅ **DONE** — Added opt-in `strict=False` kwarg to `grade()`. Under strict mode every scored `WARN` is treated as `FAIL` for both severity scoring (`SEVERITY_SCORE["FAIL"]` = 0 instead of 1) AND `has_fail` detection (which trips `_band`'s D/F floor). Classification (hardening vs correctness) is unchanged — strict tightens the grade, does not reclassify findings. `Finding.status` on the report is untouched so section-level UI still reads "WARN"; only the aggregate score changes. CLI surface: `--strict` flag added via `_build_parser()` (refactored from inline argparse so the parser is unit-testable). Default behaviour is byte-identical to pre-G4 output — verified by a `grade(rep) == grade(rep, strict=False)` assertion in the test suite. Hardening bucket `_band` call intentionally still passes `has_fail=False` in both modes (adoption ratio, not pass/fail), so a partial-adoption domain like google.com stays at B rather than crashing to D; strict mode still bites via `_score()` promoting WARN→FAIL point values within that ratio. Tests: `tests/test_grade_strict_mode.py` (6 tests: default is not strict, strict is opt-in, WARN promoted to has_fail, hardening classification preserved, all-PASS reports stay A, CLI `--strict` flag). Web surface intentionally deferred — audit did not specify UX, and a query-parameter option can be added when operator dashboards ask for it (candidate follow-up).

## False Positive Risks

- **FP1** — ✅ **DONE (pointer to C4 — already fixed)** — The ASN-table classifier in `checks.py` (`LARGE_ANYCAST_ASNS`, `_is_large_anycast_operator`) treats the ASN as ground truth and only falls back to the brand-string set when no ASN is available. VergeCloud (AS141383) sits in the ASN table so vergecloud.com is now correctly classified as one anycast operator. Pinned by `tests/test_anycast_classifier.py` (8 tests including two vergecloud-specific ones).
- **FP2** — ✅ **DONE (pointer to C4 — already fixed)** — Same fix: the ASN-first classifier means any anycast provider with a known ASN in `LARGE_ANYCAST_ASNS` is recognised without needing a brand-string entry. New providers are added by ASN (verifiable), not by pattern-matching hostnames.
- **FP3** — ✅ **WITHDRAWN (pointer to C5)** — Same withdrawal as C5: RFC 7208 §4.6.4 counts each DNS-term mechanism (including `mx`) as exactly 1 toward the primary 10-lookup limit. There is no undercount to fix. `tests/test_spf_lookup_counting.py` pins the RFC-compliant `mx=1` behaviour so a future well-intentioned "fix" cannot silently diverge from the RFC.
- **FP4** — ✅ **DONE** — `posture/emailauth.py::_txt_records` collapsed every `ok:False` from `query()` except `TRUNCATED_NO_TCP` into an empty list, which downstream (SPF/DMARC/MTA-STS/TLS-RPT/DKIM) reads as "record not published" — a rule-1 collapse of `unretrievable`→`broken` on the tool's most user-visible finding class. Widened the fix: any `ok:False` return except `NXDOMAIN` raises `TxtUnretrievable` with the underlying error name in the note; NXDOMAIN (domain does not exist) and `NoAnswer` (record confirmed absent) continue to return `[]`. Real trigger paths this catches: public resolver rate-limits (`SERVFAIL`), corporate UDP/53 packet loss (`TIMEOUT`), and rare `DNSException` subclasses bucketed as generic transport errors. Guarded by `tests/test_txt_unretrievable_wider.py` (7 tests: SERVFAIL / TIMEOUT / generic transport-exception fire; TRUNCATED_NO_TCP regression guard; NXDOMAIN + NoAnswer + normal-answer paths preserved).
- **FP5** — ✅ **DONE** — Honest correction to the audit narrative: RFC 8659 §3 says the issuing **CA** walks up the label tree (not the resolver), and honors the first CAA set it finds. So a subdomain IS constrained by its parent's CAA in practice. The pre-FP5 check only queried the exact input name, so `www.example.com` (no CAA) with `example.com` publishing `0 issue "digicert.com"` was reported as `CAA record: WARN — any public CA may issue`, which is factually wrong (DigiCert-only issuance IS enforced by the parent's CAA under RFC 8659 §3) — a rule-1 collapse of "already covered" into "broken" on the hardening axis. Fix: on empty answer, walk parents ≥2 labels toward the root, stopping short of the eTLD (probing `com` is not worth an extra query on every check). Inherited CAA emits PASS with `inherited from <ancestor>: <records>` in the detail plus a why-line pointing at RFC 8659 §3. Genuine full-chain absence still emits the RFC-8659 WARN (regression guarded). Guarded by `tests/test_caa_parent_walkup.py` (5 tests: target-has-CAA regression, single-parent inheritance surfaces PASS naming the ancestor, no-CAA-anywhere still WARNs, walk-up stops at eTLD, deep-subdomain reaches grandparent).

## False Negative Risks

- **FN1** — ✅ **RESOLVED (pointer to C5 — audit was mistaken)** — Audit pointed at C5 which was later **withdrawn** as an RFC-misread: RFC 7208 §4.6.4 counts every DNS-term mechanism (`include`, `a`, `mx`, `ptr`, `exists`, `redirect`) as exactly 1 toward the primary 10-lookup limit. The `mx` mechanism does **not** cost `1 + N` (see the ✅ **RE-EVALUATED** block on C5 above). Because the FN1 concern only exists if C5 were true, FN1 has no underlying bug. `tests/test_spf_lookup_counting.py::test_bare_mx_counts_as_one_per_rfc_7208_4_6_4` and `test_mx_with_target_counts_as_one_per_rfc_7208_4_6_4` pin the RFC-compliant behaviour so any future well-intentioned "fix" that reintroduces the undercount narrative will fail those tests loudly.
- **FN2** — ✅ **DONE** — Added six selectors documented in ESP setup guides and observed on live customer domains: `s2048` (Yahoo/AOL legacy + generic 2048-bit key marker), `ml1` and `ml2` (MailerLite — customers publish both), `mxvault` (Mailgun shared-IP selector), `salesforce` (Salesforce marketing cloud), and `postmarkapp` (Postmark's newer selector alongside `pm`, which was already present). Zoho's `zoho` selector was already in the list (audit was inaccurate on that one). Each addition is a documented ESP setup step, not a guess. Additive-only — the previously-shipped selectors are preserved (regression guarded). Guarded by `tests/test_dkim_selector_coverage.py` (4 tests: all previous selectors preserved, new selectors probed, no duplicates, each entry is a bare label without `._domainkey` suffix). A future customer-list-driven expansion (INVESTIGATION.md follow-up) can layer more selectors on top of this baseline.
- **FN3** — ✅ **DONE** — RFC 7489 §7.1 External Destination Verification. A DMARC record like `rua=mailto:reports@third-party.example` at `example.com` only works if `third-party.example` opts in by publishing a TXT at `example.com._report._dmarc.third-party.example` starting with `v=DMARC1`. Without that opt-in, mail receivers refuse to send aggregate reports and the DMARC deployment looks configured but silently drops reports. Pre-FN3 the tool only checked whether `rua=` was present — a typo'd or expired third-party destination PASSed. Fix: new `emailauth.evaluate_dmarc_reporting(domain, rua, ruf)` parses each mailto: URI (including RFC 7489 §6.2 `!<size>` suffix), classifies internal vs external destinations, and probes the ExtDestVerification TXT for each external one. `checks.py` emits one finding per external destination: PASS on verified opt-in, FAIL on confirmed absence, UNKNOWN when the verification lookup itself was unretrievable (rule 1 — must not collapse "could not verify" into "not verified"), WARN on unparseable / non-mailto: schemes. Guarded by `tests/test_dmarc_external_destination.py` (9 tests covering internal exemption, verified, unverified, unretrievable, non-DMARC1 TXT, multi-destination mix, ruf verification, empty rua/ruf, and malformed URIs).
- **FN4** — ✅ **DONE** — `_dnssec` now inspects the observed algorithm set and emits a WARN finding labelled `Algorithm strength` when any RFC 8624 SHA-1-based signing algorithm (1 RSAMD5, 3 DSA, 5 RSASHA1, 6 DSANSEC3SHA1, 7 RSASHA1NSEC3SHA1) is in play. Finding is `hardening=True` — an operator who signed the zone with a weak alg still adopted DNSSEC and should not grade *worse* than one who never signed at all; the intended signal is "rotate to alg 13 (ECDSAP256SHA256) or 15 (ED25519) at your DNS provider" and the remediation text says exactly that. Modern algs (8, 13, 14, 15, 16) never trigger. Mixed rollover state (deprecated + modern KSK simultaneously) still WARNs — until the deprecated key is retired, validators still consume the weak signature path. Tests: `tests/test_dnssec_deprecated_algorithms.py` (10 tests: 5 deprecated → WARN, 3 modern → silent, empty algs → silent, mixed → WARN naming only the deprecated one, hardening=True pin).
- **FN5** — ✅ **DONE** — `open_resolver_check` used `ip = (ips.get("ipv4") or [None])[0]` and, on absent v4, emitted `{"tested": False, "reason": "no A record"}` — leaving IPv6-only nameservers *silently* skipped and dual-stack NSes tested only over v4. Two false-PASS classes: (a) a v6-only NS whose v6 stack served open recursion to the whole IPv6 internet, and (b) a dual-stack NS where the v4 gateway blocked external recursion but the v6 gateway ACL was never wired up (a real, common misconfiguration during v6 rollout). Fix: `probe_ips` now contains one v4 and one v6 address when both are published; probes run for every entry (2 probes per family × 2 families = up to 4 probes on a dual-stack NS). Worst-case wins for `open` detection so a v6-open + v4-clean NS trips `any_open=True`. `subnet_variance` extended from the exact-2-probe case to any-≥2-probe disagreement — an address-family ACL divergence surfaces as WARN. Terminal error string changed from `"no A record"` to `"no address"` for the truly-addressless case so the reason no longer lies about which family was tried. Tests: `tests/test_open_resolver_ipv6.py` (5 tests: v6-only tested not skipped, dual-stack probed over both families, v6-open-when-v4-refuses flagged, v4-only regression guard, addressless NS reason doesn't lie about family). All 6 existing D5 tests still pass — the dual-probe design generalises cleanly.

## Edge Cases

- **E1** — ✅ **DONE** — Domains at the exact 63-octet label / 253-octet name boundary. v0.5 checked per-label length (63) but had NO total-length check, so a domain of 4×63-char labels (255 chars including 3 dots) would slip past `normalize_domain` and surface as an obscure `idna` / dnspython downstream error instead of a clean API-boundary rejection. Fix: added an RFC 1035 §2.3.4 total-length guard in `posture/core.py::normalize_domain` (after IDN encode, before the per-label loop) — raises `ValueError` naming the octet count and the section number when `len(puny) > 253`. Placed before per-label so grossly-oversized input surfaces the more informative diagnostic. Tests: `tests/test_normalize_domain.py::TestBoundaryLengths` — 3 cases: 253 accepted (3×63 + 1×61 + 3 dots), 254 rejected with 253/total-length message, and a belt-and-braces 63-char label at a non-leading position. Full offline suite: 233 passed, 0 regressions.
- **E2** — ✅ **DONE** (coverage-only) — Bidirectional IDN (Arabic/Hebrew labels): punycode round-trip. Honest note: no code change was needed. Live probes of `normalize_domain` with `يمن.ye`, `مثال.إختبار`, `ישראל.co.il`, `טעסט.co.il` all return the expected `xn--` puny form via the existing IDN branch. The audit item was a test-coverage gap, not a latent bug — the RTL round-trip already works because CPython's `idna` codec handles the script-independent punycode encoding correctly, and `normalize_domain` doesn't have RTL-specific logic that could get it wrong. Fix: added `TestBidiIdn` in `tests/test_normalize_domain.py` (4 tests): Arabic label with fixed ground-truth puny, Arabic-label-and-Arabic-TLD (two `xn--` segments guard), Hebrew label under `.co.il`, and an ASCII-only assertion on the puny form (the string that hits DNS). Tests are regression pins — if a future refactor introduces script-specific handling that mangles RTL, the ASCII-only pin will catch it.
- **E3** — 🟨 **N/A** — Empty non-terminal in DNSSEC NSEC/NSEC3 responses. Honest audit: this item does not apply to the tool's usage pattern. `posture/dnsmod.py::dnssec_status` queries DS (parent), DNSKEY+RRSIG (child), runs `dns.dnssec.validate` for self-signature and `dns.dnssec.make_ds` for chain-of-trust, then checks the resolver's AD bit. **The tool never parses NSEC or NSEC3 records directly** — authenticated denial-of-existence is consumed internally by the validating resolver, and the tool only reads the AD flag / SERVFAIL rcode it returns. Empty non-terminals (ENTs) — names with no records but with children — only exist at intermediate labels, whereas `normalize_domain` always yields an apex (`example.com`, not `foo.sub.example.com`), so an ENT can never be the query target. If someone forced an ENT as input, DNSKEY/DS would come back NODATA and the tool would correctly report DNSSEC as "not published" (rule 1 preserved: not-applicable → not-applicable). No test needed; the scenario is unreachable through the API boundary.
- **E4** — ✅ **DONE** (coverage-only) — Wildcards in DKIM (`example.com` has `*._domainkey.example.com` per CLAUDE.md ground truth). Honest audit: the audit description was misleading. `evaluate_dkim` **already handles wildcards correctly** via the canary probe at `posture/emailauth.py:173-189` — a `probe<uuid>._domainkey.<domain>` lookup runs first, and if it responds the function short-circuits with `wildcard=True` and the derived `key_state` (revoked vs valid), NEVER running the per-selector fanout. Without this guard, `example.com` would report DKIM found under all 26 selectors when it is actually publishing a revoked key. The audit item was a test-coverage gap, not a bug. Fix: `tests/test_dkim_wildcard.py` (5 tests): wildcard with empty p= reports `revoked` and labels the RFC 6376 §3.6.1 state; wildcard with valid key reports `not meaningful`; canary short-circuit skips per-selector probing entirely (`probed=0`, only 1 DNS call); negative-space test that a real per-selector match still works when the canary misses; and a rule-1 pin that "no selectors matched" surfaces as "Not found under common selectors" (never collapses into "DKIM absent").
- **E5** — ✅ **DONE** — `evaluate_dmarc` now returns `multiple=True` + `all_records=[...]` when >1 `v=DMARC1` record is present at `_dmarc.<domain>`. `_email` emits an explicit `FAIL` labelled `DMARC` with the record count and the RFC 7489 §6.6.3 citation ("receivers apply no DMARC-based decision"). Critically: when the multiple-records branch fires we do NOT also emit the normal `DMARC policy` PASS/WARN row parsed from `txts[0]` — that was the specific false-signal the fix exists to prevent (a PASS for `p=reject` on a domain whose DMARC validators are actually ignoring). Non-DMARC TXT records at the same name (e.g. an SPF record accidentally placed under `_dmarc.`) don't count toward the multiple check — only records that start with `v=DMARC1` are enumerated. Tests: `tests/test_dmarc_multiple_records.py` (5 tests: evaluate_dmarc flags multiple, single-record path unchanged, non-DMARC TXTs ignored, multiple emits FAIL with RFC citation, multiple does NOT also emit a policy PASS).
- **E6** — ✅ **DONE** — IPv6-only nameservers. v0.5 `probe_each_ns` did `ip = (ips.get("ipv4") or [None])[0]` and, if no IPv4 was found, emitted `reachable=False, error="no_A_record"` — even when the NS published a working AAAA. That's a rule-1 violation: an IPv6-only-but-reachable NS was reported as broken. Fix: fall back to `ips["ipv6"][0]` when `ipv4` is empty (`dns.query.udp` accepts a v6 literal). Also renamed the terminal error from `no_A_record` to `no_address` for the truly-no-addresses case, so the label doesn't lie about which address family was checked. Tests: `tests/test_probe_each_ns_ipv6.py` (3 tests): IPv6-only NS probed over v6 and reported reachable with the AAAA as the recorded IP, dual-stack NS still prefers v4 (historical behaviour pin), NS with neither A nor AAAA remains a FAIL with `error=no_address`. Full offline suite: 245 passed.
- **E7** — ✅ **DONE** — Non-ASCII in RDAP vCard fn: Unicode normalisation. Two real gaps in v0.5 `_extract_vcard_fn`: (a) an RDAP registry can serialise the same visible name in NFC (`Café`, é as U+00E9) or NFD (`Cafe` + combining acute U+0301) — byte-identical *and* logically-identical are not the same under Unicode, and any downstream dedupe / whitelist / cache-key logic would treat the two forms as different values; (b) some payloads embed CRLF or padding in the fn text, which propagates control characters into terminal / HTML output. Fix: NFC-normalise via `unicodedata.normalize("NFC", …)` and `.strip()`; a value that's non-string (some emitters ship a list at `item[3]`) returns None instead of leaking a list downstream; whitespace-only becomes None so the caller falls back to `entity.handle`. Tests: `tests/test_vcard_fn_normalisation.py` (6 tests): NFD→NFC (load-bearing), NFC round-trip idempotency, whitespace stripping, Devanagari (BFSI focus) passes through cleanly, whitespace-only returns None, non-string doesn't raise. Full offline suite: 251 passed.

## Security Issues

- **S1** — ✅ **DONE (pointer to C1 — fixed in commit `6bf64a9`)** — `renderFinding` in `web/static/app.js` now whitelists `f.status` through `safeStatus()` before interpolation into the badge class attribute; the returned value is one of `PASS|WARN|FAIL|INFO|UNKNOWN`. `f.label`, `f.detail`, `f.why`, and domain `name` all go through `escapeHtml()`.
- **S2** — ✅ **DONE** — Audit brief was slightly imprecise on details (the code shipped `allow_credentials=True` in the audit note, but the actual v0.5 middleware ran with credentials **defaulted to False** — still `allow_origins=["*"]` and `allow_methods=["*"]` though). Fixed by making CORS opt-in via `POSTURE_ALLOWED_ORIGINS`: default (env unset or empty) attaches NO `CORSMiddleware` at all → same-origin only, no ambiguity from `allow_origins=[]` still emitting preflight machinery. Explicit env value is comma-separated origins (`https://vergecloud.com,https://www.vergecloud.com`); parsed with whitespace stripping, empty entries dropped. A literal `*` anywhere in the list raises `ValueError` at startup — banishing `*` is the whole point; silent fallback would reintroduce the bug. Constricted methods to `["GET", "POST"]` (the only two used by the SPA) and headers to `["Content-Type"]`; `allow_credentials=False` explicit. Refactored app construction into `_build_app()` factory so the tests can spin up a fresh app under a controlled env state without stomping the module-level `app` uvicorn imports. Tests: `tests/test_web_cors_allowlist.py` (6 tests: default no middleware, wildcard rejected, wildcard-inside-list rejected, explicit origins passed verbatim, whitespace stripped, empty-string treated as unset).
- **S3** — ✅ **DONE** — see F3/S3 combined entry above.
- **S4** — ✅ **DONE** — Replaced `DOMAIN_RE` (an anchored regex of the ReDoS-friendly `([...]+)+` shape) with `_is_valid_domain(puny)`, a linear-time label-by-label validator. The input at this callsite is already ASCII punycode (upstream `normalize_domain` runs `idna` first), so a regex was overkill anyway — plain string checks are O(n) by construction and independent of the `re` module's engine. Contract preserved: 1–253 char overall, 1–63 char labels, no leading/trailing hyphen, alnum + `-` only, TLD ≥ 2 chars with at least one letter (so `192.0.2.1` still fails at the helper level even though upstream also catches it). Removed the now-unused `import re`. Tests: `tests/test_web_domain_validation_linear.py` (16 tests: 14 correctness + 2 wall-clock ReDoS ceilings on 5000-char hostile input and 100-label chain — both must complete in < 0.5s). Full offline suite of 189 tests still passes, so no regression in the existing `test_web_validation.py` accept/reject pins.
- **S5** — ✅ **DONE** — see S5+S6 combined entry below.
- **S6** — ✅ **DONE** — S5 and S6 fixed together with a single `_security_headers_middleware` on `_build_app()`. CSP: strict — `default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data:; connect-src 'self'; font-src 'self'; object-src 'none'; frame-ancestors 'none'; base-uri 'none'; form-action 'self'`. No `'unsafe-inline'`, no `'unsafe-eval'` (the SPA was audited to be free of inline scripts / inline styles / inline event handlers, so the strict form is achievable without escape hatches — future regressions must justify adding them). Other headers: `X-Content-Type-Options: nosniff`, `Referrer-Policy: no-referrer` (domain names are moderate-PII in a shared-link context; no need to leak them to third-party resources), and an explicit `Permissions-Policy` denying geolocation / microphone / camera / usb / payment / accelerometer / gyroscope / magnetometer / autoplay / fullscreen — deliberately enumerated (not wildcarded) for reviewability. Middleware uses `headers.setdefault` so a route that wants to override a header for a legitimate reason can, but the default is always applied. Body untouched. Tests: `tests/test_web_security_headers.py` (11 tests: presence on index/static/JSON API, CSP shape assertions for `default-src 'self'`, absence of `'unsafe-inline'/'unsafe-eval'`, `frame-ancestors 'none'`, `base-uri 'none'`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: no-referrer`, `Permissions-Policy` denying the sensitive APIs, and a body-integrity check that Content-Length still matches). Fixture patches `check_environment` so `/healthz` stays offline. `httpx` added as a test-time dep for `fastapi.testclient.TestClient`.
- **S7** — ✅ **DONE** — Added `SSE_MAX_STREAM_SECONDS = 120` module constant and a wall-clock ceiling inside `stream_check`'s generator. When the deadline passes, the generator emits `event: timeout\ndata: {"reason": ..., "max_seconds": ...}\n\n` and returns cleanly; the underlying `Job` is preserved so the client can reconnect or fetch `/api/check/{id}/result` (its own retention is still governed by `CHECK_JOB_TTL_SECONDS`, unchanged). The `asyncio.wait_for` inside the wait loop now uses `wait_budget = min(15.0, deadline - now)` so a completely-idle stream still terminates at the ceiling (previously it would spin on 15s waits forever). Ceiling is tunable via monkeypatch for tests. Tests: `tests/test_web_sse_timeout.py` (4 tests: constant exists and is bounded, slow stream terminates with `timeout` event within 3× ceiling, timed-out job stays in `JOBS`, happy-path stream still ends with `event: end` not `timeout`). `pytest-asyncio` added to `[dev]` extra and `asyncio_mode = auto` set in `pytest.ini`.
- **S8** — ✅ **DONE** — Added `_maybe_show_first_run_banner()` in `posture/cli.py`, invoked at the top of `main()` before any check runs. Banner enumerates the traffic categories (recursive DNS, authoritative NS, RDAP, AXFR, HTTPS/MTA-STS) and lands on **stderr** so `--json` consumers piping into `jq` are unaffected — this was a specific concern from CLAUDE.md's "byte-for-byte identical" invariant between CLI and web. Ack file at `XDG_CONFIG_HOME/posture-checker/first-run.ack` (falling back to `~/.config/...`); once present, banner is silent. `POSTURE_ACK_FIRST_RUN=1` in the env suppresses the banner AND deliberately does NOT persist an ack — a CI / scripted run should not eat the ack for the interactive user sharing the machine. Read-only `$HOME` (some container images) gracefully skips the ack write via `OSError` catch — banner reprints next time rather than crashing the CLI. Tests: `tests/test_cli_first_run_banner.py` (5 tests: first run banner on stderr naming DNS/RDAP traffic, ack file created after first run, `--json` stdout stays banner-free, second run silent, env var suppresses without writing ack).
- **S9** — ✅ **DONE** — Audit brief was partially right, deeper bug found while implementing: TWO `requests.get(RDAP_IP_BOOTSTRAP_URL, ...)` calls in `posture/core.py` (lines 312 + 320) and the `_load_bootstrap` fetch on line 152 were passing NO headers at all — they went out with the default `python-requests/X.Y.Z` UA, not even the stale `HEADERS` constant. Fixed by threading `headers=UA_HEADERS` through every bootstrap call. Bumped the UA string from `vergecloud-posture-checker/0.1 (prototype)` to `posture-checker/0.5 (+https://github.com/vergecloud/posture-checker)` so third parties (IANA, ARIN, NIXI, DNS/RDAP registries) can rate-limit our tool specifically instead of an entire IP block, and have a contact channel if the traffic pattern becomes problematic. Tests: `tests/test_outbound_user_agent.py` (6 tests: UA names the tool, carries a version number, has a contact URL, is not the "prototype" stub, DNS bootstrap fetch passes headers, IP bootstrap fetch passes headers).

## Performance Issues

- **P1** — ✅ **DONE (already implemented — pointer to D1)** — Same bug, same fix — see D1 above.
- **P2** — ✅ **DONE (pointer to C3 — already fixed at baseline)** — `emailauth.evaluate_dkim` submits selector probes via `ThreadPoolExecutor.map` and materialises the future set exactly once (`list(ex.map(probe, selectors))`) — no double iteration. `tests/test_dkim_probe_count.py` pins the invariant with a `dnsmod.query` counter (each selector queried exactly once).
- **P3** — ✅ **DONE (pointer to C2 — already fixed at baseline)** — `evaluate_mta_sts` has exactly one callsite in `checks.py` (`checks.py:855`) so no double HTTPS fetch. `tests/test_mta_sts.py::test_mta_sts_queries_the_sts_name_once` pins the invariant.
- **P4** — ✅ **DONE** — `dnsmod.probe_each_ns` iterated over the NS map serially — a domain with 6 slow-to-respond NSes had a worst-case wall time of 6 × TIMEOUT (~24s). Even the common case (one lame NS in the set) blocked every subsequent probe. Fix: extracted the per-NS body into a raise-free `_probe_one_ns(domain, host, ips)` helper, then submit all probes to a bounded `ThreadPoolExecutor` (`_PER_NS_PROBE_MAX = 8`, same rationale as `_PARENT_QUERY_MAX` — cover common 4-NS/6-NS domains in one wave, don't over-fan on anycast pools). Wall time now scales with the slowest NS + fan-out overhead, not the sum. Return shape is byte-identical to the sequential version so downstream (`checks.py` emit path, JSON output, cached SSE payloads) needed no changes. Guarded by `tests/test_per_ns_probe_parallel.py` (5 tests: 6 × 0.4s stalls complete in <1.5s wall, return-shape stability, TIMEOUT bucketing preserved, no-address NS still reported, E6 IPv6-fallback regression pin).
- **P5** — ✅ **DONE (already implemented — pure test-coverage pin)** — Honest correction to the audit narrative: `core._load_bootstrap` and the parallel `_ip_bootstrap["ipv4"]` path already cache both the DNS and IP RDAP bootstrap files for 24 hours (`_bootstrap_cache["dns"] = (mapping, time.time() + 86400)`). The right-to-left label walk in `rdap_endpoint_for` reuses this cache — a 20-lookup session issues exactly one HTTPS GET. The audit was stale; no code change was needed. Added `tests/test_rdap_bootstrap_cache.py` (4 tests: cold-cache single fetch, TTL-expiry refetch, multi-label walk single fetch, TTL is ~86400s) to lock in the cache so a future refactor that "just for testing" drops the TTL to 60s or removes the cache entirely trips a red test loudly.
- **P6** — ✅ **DONE (pointer to F4)** — Same bug, same fix — see F4 above. `RESULT_CACHE` is now an LRU-bounded `OrderedDict`.

## Cross-Platform Issues

- **CP1** — ✅ **DONE** — Added `[project.scripts]` entries `posture-cli = "posture.cli:main"` and `posture-web = "web.server:main"`. After `pip install .` the tool is on PATH on every platform without any shell wrapper. Introduced a `main()` function on `web.server` that calls `uvicorn.run(...)` directly (previously the shell launcher used `uvicorn web.server:app` via CLI, which required uvicorn on PATH — the console-script entry embeds the invocation and works on Windows too).
- **CP2** — ✅ **DONE** — Added PEP 621 `pyproject.toml` at the repo root with populated name/version/description/keywords/classifiers. Build backend is `setuptools>=68`. Package discovery is explicit (`packages = ["posture", "web"]`) so the tree layout is unambiguous. `web/static/*` is included via `package-data` so the SPA assets ship inside the installed wheel and `posture-web` can serve them from any working directory. Lockfile deliberately deferred — `pip-tools` / `uv` support is a downstream tooling choice that shouldn't block CP2's install-ability outcome; the version floors on core deps are enough to guarantee compatible installs. `requirements.txt` is left in place for now (dev-loop parity) but consumers should prefer `pip install .`.
- **CP3** — ✅ **DONE** — Declared `requires-python = ">=3.10"` in the `[project]` table. Rolled into the CP2 commit because the field lives inside the same TOML file — pinned separately by a dedicated regression test that greps for the floor across 3.10+.
- **CP4** — ✅ **DONE** — Moved `fastapi`, `uvicorn`, `slowapi` into an optional `web` extra (`pip install .[web]`) — CLI-only users no longer drag the web stack in. Critically, the extra uses plain `uvicorn>=0.23`, NOT `uvicorn[standard]`, because `[standard]` pulls `uvloop` which does not build on Windows. Also added a `performance` extra that opts back into `uvloop` conditionally (`platform_system != 'Windows'`), so Linux/macOS deployments that want the perf can `pip install .[web,performance]`.
- **CP5** — ✅ **DONE** — Added a multi-stage `Dockerfile` at the repo root. Base image `python:3.12-slim` (small, reproducible; alpine would break `cryptography` wheels). Builder stage runs `pip install .[web]` against a `/install` prefix so the runtime stage can `COPY --from=builder /install /usr/local` without dragging build caches. Runs as a dedicated non-root user `app` (uid/gid 1000, `/sbin/nologin` shell) — the tool takes arbitrary domain input and hits arbitrary third-party infra, so dropping privileges limits any future escape/write-bug blast radius. `EXPOSE 8000` telegraphs the port for `docker run -P` and Fly.io / Cloud Run. `CMD ["uvicorn", "web.server:app", "--host", "0.0.0.0", "--port", "8000"]` — deliberately NOT the `posture-web` console script because that entry binds `127.0.0.1` (correct for local dev, wrong inside a container where the process must listen on the external interface). Tests are structural assertions over the Dockerfile text (base image, python floor, `pip install .[web]`, non-root USER, EXPOSE 8000, CMD starts the web tool) rather than `docker build` invocations — pulling docker into pytest isn't justified by the dev-laptop and CI bar. A build-and-run smoke test can live in a downstream CI workflow.
- **CP6** — ✅ **NO CHANGE NEEDED — audit inaccurate** — Full sweep of the codebase for `os.path.join`, `+ "/"`, and any hardcoded filesystem-path concatenation returns **zero** cross-platform issues. The only two hits — `posture/core.py:160` and `posture/core.py:335` — are RDAP **URL construction** (`base.rstrip("/") + "/"`, `base.rstrip("/") + f"/ip/{ip}"`). URL path separators are protocol-mandated (RFC 3986 §3.3) regardless of host OS and must NEVER be replaced with `pathlib.Path` or `os.sep`. The only filesystem-path site in the entire tool is `web/server.py:304` — `Path(__file__).parent / "static"` — which is already correct pathlib usage. Nothing to migrate. Recorded here rather than silently dropped so a future refactor doesn't reintroduce the audit item.
- **CP7** — ✅ **DONE** — Preserved `cryptography>=42` from `requirements.txt` inside `[project.dependencies]`. 42+ ships prebuilt wheels for all target Pythons on Windows/macOS/Linux, so `pip install .` never falls back to a Rust source build. This is the dependency D4 depends on (`_HAS_CRYPTOGRAPHY` probe) — the version floor now travels with the packaging metadata rather than a floating `requirements.txt` that consumers might not read. Test: `test_cryptography_dependency_has_version_floor_for_wheels`.

## Web/UI Issues

- **W1** — ✅ **DONE (pointer to C1)** — Same fix as S1/C1: `web/static/app.js:239-241` uses `safeStatus()` whitelist for the badge class and `escapeHtml()` for every user-visible text field.
- **W2** — ✅ **DONE** — SSE stream reconnection UI. On the `error` event `app.js` now renders an inline Retry button inside the `#status` region (which is aria-live=polite, so screen readers announce it). The Retry button re-dispatches the form's submit event so the same domain is re-scanned. The user no longer stares at a half-populated table wondering whether the scan is still running. Regression pin: the SSE error handler must contain the word "retry" and the DOM must expose a Retry surface.
- **W3** — ✅ **DONE** — Mobile viewports (<640px). The `.section-head` used `display: flex; justify-content: space-between` with no wrap, so a long section title collided with the grade pill on narrow screens. Fix: inside the `@media (max-width: 640px)` block, add `flex-wrap: wrap; gap: 4px 12px` on `.section-head` and `flex-shrink: 0` on `.grade-pill`. Regression pin: the mobile media query must contain a `section-head` or `flex-wrap` reflow rule.
- **W4** — ✅ **DONE** — Dark-mode chrome. Honest correction: the palette IS dark, so the audit's "no dark-mode support" wording is off; the real gap is that `color-scheme` was not declared, so Chromium rendered its scrollbars and native form widgets in *light* mode against the dark UI. Fix: added `color-scheme: dark` to `:root` (1 line). Full light-mode variant is out of scope for the POC. Regression pin: `:root` must declare `color-scheme: dark`.
- **W5** — ✅ **DONE** — Surface degraded environment on landing. `/healthz` was already returning 503 with a JSON payload naming the interception / TCP-53 / AA-flag issues detected by `posture.selftest.check_environment`, but the front-end never called it — the user submitted a scan and only saw "UNKNOWN" findings, with no cue that the *cause* was network-path degradation on the server rather than on the target domain. Fix: `app.js::preflightEnvironment` fetches `/healthz` on load; if the response is non-200 or `status != "ok"`, it renders the new `#envBanner` (styled with a warm-yellow warning border, role="alert", aria-live="polite") above the form. Banner enumerates the specific issues (`intercepted`, `aa_flag_trustworthy=false`, `tcp53_direct=false`) plus the `notes` array from the payload. A network failure hitting our *own* /healthz surfaces as "endpoint unreachable" — that's itself a useful signal. Regression pins: `#envBanner` DOM anchor exists (with aria-live), starts hidden, `app.js` references `/healthz`, and the code path reads `environment` / `notes` from the payload rather than showing a hard-coded string. Full offline suite: 268 passed.
- **W6** — ✅ **N/A — no reachable CSRF surface** — Post-S2, CORS middleware is unattached by default (same-origin only) and even when opted in via `POSTURE_ALLOWED_ORIGINS` runs with `allow_credentials=False`. There are no cookies, no session tokens, no authenticated state — the API is anonymous and rate-limited by client IP (S3/F3 fix). A CSRF-style attack forcing a victim's browser to POST `/api/check` would consume the *victim's* rate-limit quota (nuisance-level DoS, not a security compromise) while gaining the attacker nothing they couldn't get by calling the API directly from their own server. CSRF tokens defend authenticated-session APIs; this tool has no session. Recorded here rather than silently dropped so a future auth-adding refactor re-visits the issue on its merits.

## Accessibility Issues

- **A1** — ✅ **DONE** — Muted-text contrast. Honest correction: audit described the site as light-themed ("yellow-on-white / grey-on-white") but the UI is dark-themed (`--bg: #0e1218`). The real gap: `--muted: #8896a8` was used for `.finding .detail` and `.finding .why` at 12–13 px small text, hovering right at the WCAG AA 4.5:1 line against `#171d26`. Fix: brightened `--muted` and `--info` to `#b0bccd` (~7:1 against the card bg — plenty of margin). Regression pin in `tests/test_web_accessibility.py` requires each channel of `--muted` be ≥ `0xA0`.
- **A2** — ✅ **DONE** — Focus outline. Audit said "scan button" but the actual removal was `input[type=text]:focus { outline: none; }` in `style.css`. The button used the browser default and was fine. Fix: added a global `:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }` so keyboard users get a visible ring but pointer clicks stay ring-free. Regression pin: `:focus-visible` must be present in `style.css`.
- **A3** — ✅ **DONE** — Live regions. `#status` now carries `role="status" aria-live="polite"`, `#sections` carries `aria-live="polite" aria-relevant="additions"`, and `.header-grade` carries `aria-live="polite"` so the final overall grade is announced. Screen readers now hear each SSE-streamed section as it lands. Regression pins verify the attributes on both nodes.
- **A4** — ✅ **DONE** — Semantic markup. `app.js::prepareSections` now emits `<h2>` for the section title (was `<span>`) so screen-reader users can navigate by heading, and `.section-body` is now a `<ul>` populated with `<li class="finding">` rows (was flat `<div>`s). Badges carry `aria-label` so their compact PASS/WARN/FAIL/INFO text is announced verbatim. CSS `.section-body { list-style: none; margin:0; padding:0 }` keeps the grid layout intact, and `.section-head h2` resets browser h2 typography so the visual row is unchanged.
- **A5** — ✅ **DONE** — Domain input has `<label for="domain" class="sr-only">Domain name to check</label>`. `.sr-only` visually-hides the label without removing it from the accessibility tree (RFC-ish standard clip pattern). Regression pin greps for the `<label for="domain">` binding.
- **A6** — 🟨 **N/A** — Language attribute was **already present**: `web/static/index.html:2` reads `<html lang="en">`. The audit was wrong. Regression pin locks it in so a future rewrite can't drop it silently.

## Test Coverage Gaps

- **T1** — ✅ **DONE** — Prior coverage sliced individual pieces of the check engine (DNSSEC state machine, SPF lookup counting, per-NS probing, anycast classifier call site, grading fixtures, per-section renderers) but the *orchestrator itself* was unpinned — `run()`'s 7-section pipeline, its exception isolation, the NXDOMAIN short-circuit, the rule-5 environment gate, and `run_streaming()`'s event contract that `web/server.py` consumes to drive the SPA. A refactor that reordered the event stream or dropped an event type would break the SPA silently with no red run. New file `tests/test_checks_run_orchestration.py` (11 tests) patches every DNS/RDAP/emailauth entry point at the `posture.checks` module boundary (`check_environment`, `rdap_lookup`, `ip_rdap`, `dnsmod.{domain_exists, get_ns_and_ips, parent_delegation, probe_each_ns, get_soa, query, dnssec_status, axfr_open_check, open_resolver_check, authoritative_vs_cached}`, `emailauth.{evaluate_spf, evaluate_dkim, evaluate_dmarc, evaluate_dmarc_reporting, evaluate_mta_sts}`) with a benign default harness — sections tolerate the "no data / not present" defaults cleanly and emit WARN/INFO rows, isolating what's under test to the orchestrator's decision tree. Pins: NXDOMAIN → single FAIL row in Registration, no other sections run (both run() and run_streaming()); happy-path streaming emits `started → environment → 7 sections → complete` in order with expected keys in the `complete` payload; run() and run_streaming() produce byte-for-byte identical `findings` (the CLAUDE.md module-map contract); a section that raises becomes UNKNOWN + `rep.degraded` entry and downstream sections still run (both entry points); degraded environment routes per-NS reachability to UNKNOWN (rule-5 pin, rule-1 pin — never collapses to FAIL); env notes surface in the streaming `environment` event; unparseable input yields a terminal `error` event with `type: input` (not a crash); Report public attribute contract pinned.
- **T2** — ✅ **DONE** — Grading is now covered by four fixture-based test files: `tests/test_grade_strict_mode.py` (strict-mode WARN→FAIL promotion), `tests/test_grade_split_surfaced.py` (correctness/hardening split surfaced through `grade()` output), `tests/test_grading_google_unsigned.py` (google.com — DNSSEC-unsigned-by-choice must be A overall, guards the HARDENING_ABSENCE routing that resolved the false-D), and **new** `tests/test_grading_ground_truth.py` (10 tests spanning the remaining three CLAUDE.md ground-truth domains — cloudflare.com all-PASS→A/A/A, dnssec-failed.org DNSSEC-broken must land correctness sub-A and section-band D/F, vergecloud.com 1-operator anycast→A overall pinning the grade-side of the C4 fix). The DNSSEC-router invariant is now load-bearing in both directions: `test_google_unsigned_correctness_is_clean_A` (state=not_configured → hardening bucket) AND `test_dnssec_failed_correctness_fails` (state=broken → correctness bucket) — refactoring one without the other will trip a red run.
- **T3** — ✅ **DONE (with correction)** — The audit line named `/scan` and `/scan/stream`, but those routes don't exist — the endpoints are `POST /api/check`, `GET /api/check/{id}/stream`, `GET /api/check/{id}/result`, and `GET /healthz`. Prior coverage sliced the web layer (CORS parsing, security-header middleware, SSE timeout ceiling, rate-limit key derivation, env-banner placeholder, XSS in status strings, `_validate_domain` direct-call) but no test exercised the HTTP contracts themselves — a shape change in the response bodies or a route rename would break `web/static/app.js` silently with no red run. New file `tests/test_web_endpoints_integration.py` (18 tests) uses `TestClient` with `patch.object(server, "run_streaming", ...)` and `patch.object(server, "check_environment", ...)` to keep every test offline while driving the real ASGI pipeline: `/healthz` returns 200 with `status: ok` clean vs 503 with `status: degraded` on `safe_for_per_ns_checks=False` (rule-5 gate pin); `POST /api/check` returns `{check_id, cached: False, domain}` on happy path and `{check_id, cached: True, domain, result: <complete-event>}` on a repeated submit within TTL; parametrised validation rejection covers URL/path/email/IPv4/single-label (400) plus missing-domain-field (422) and oversized-domain (422); `GET /api/check/{id}/result` returns 404 on unknown id, the `complete` event payload on a finished job, and the `{error, events}` soft-failure shape when the worker set `job.error` but never emitted `complete`; `GET /api/check/{id}/stream` returns 404 on unknown id and, on a pre-populated done job, replays every queued event with `event: end` as terminator (missing `end` would leave the SPA's EventSource open forever). HTTP-method contract pinned separately (GET on `/api/check` → 405, POST on `/result` → 405) so a refactor can't accidentally open CSRF-friendly surfaces. Fixtures snapshot and restore `JOBS` + `RESULT_CACHE` so cross-test state leakage is impossible.
- **T4** — ✅ **DONE** — `check_environment` is the CLAUDE.md rule-5 gate: when it reports the wire path is untrustworthy, every per-NS finding downstream MUST degrade to UNKNOWN. A regression that silenced a note or changed the return-shape keys could re-enable false-positive findings on corporate networks with transparent DNS proxies — the exact failure mode the module was written to catch. New file `tests/test_selftest_check_environment.py` (10 tests) fault-injects each capability leg by patching `dns.query.udp`, `dns.query.tcp`, and `dns.resolver.resolve`: clean env → safe=True, blackhole-IPs-answering → intercepted+safe=False, partial interception → still intercepted, AA-stripped control response → aa_flag_trustworthy=False, UDP/53 timeout → udp53_direct=False, non-Timeout exception on control query → typed note, TCP/53 block alone → tcp53_direct=False but safe=True (UDP-first semantics), combined faults → every leg's note surfaces. Shape contract pinned separately: `set(env.keys())` must equal the 6-key set that CLI + web renderers consume. Resolver-lookup-failure path pinned too — a fully-broken system must still return a valid dict, not raise.
- **T5** — ✅ **DONE** — Existing tests covered slices (AD-bit fallback, unretrievable-probe L4 gates, no-cryptography path, deprecated-algorithm hardening) but the state derivation itself was implicit — the ladder mapping 6 atomic signals `(ds, dnskey, self_signed, ds_matches_dnskey, ad_authenticated, ds_probe_responded, dnskey_probe_responded)` → one of `{validating, broken, incomplete, not_configured, unknown}` was never directly tested. Extracted a pure `_dnssec_derive_state` helper (byte-for-byte-equivalent mechanical refactor of the tail of `dnssec_status`) so the branch logic could be pinned without needing to fake `dns.query.udp` + `dns.dnssec.validate` + `dns.dnssec.make_ds` in every test. New file `tests/test_dnssec_state_machine.py` (14 tests): validating (green + AD-inconclusive), broken (DS-orphan / DS-DNSKEY-mismatch / self-sig-fail / AD-rejects-despite-crypto-OK), incomplete (child-signs-parent-doesn't-anchor), not_configured (positive absence on both), unknown (both L4 gates), plus precedence tests (crypto-fail beats AD=True) and an exhaustive combinatorial guard that walks all 3^3 · 2^4 = 432 input shapes and asserts every one returns one of the five known state strings — locks the ladder against a refactor that leaves an input shape unmapped.
- **T6** — ✅ **DONE (with correction)** — The audit line asked for tests of the "right-to-left label walk" and named `.in` / `.co.in` as the regression risk. Writing the tests surfaced that the walk is actually **shortest-first (TLD-first)**, not longest-first — which is *correct* for DNS-RDAP bootstrap per RFC 7484 §3 (bootstrap keys are single labels). RDAP-IP bootstrap (§5) is longest-match on CIDR but sits in a different code path (`_ip_bootstrap` / `ip_rdap`). New file `tests/test_rdap_endpoint_walk.py` (9 tests) patches `_load_bootstrap` to an offline mapping and pins: `com` resolves at the TLD; `example.co.in` lands on the `.in` NIXI entry (the CLAUDE.md guardrail); walk is shortest-first even when a multi-label key would shadow (defensive — such a key would not appear in valid IANA bootstrap but the walk order should be deliberate); multi-label domains match at the TLD; unknown eTLDs return `(None, None)` (used by callers for rule-1 UNKNOWN routing, NOT FAIL); `indianbank.bank.in` lands on NIXI. Uppercase-input contract is loose (may return None or match) — the walk sees pre-normalised punycode from `normalize_domain`, so exact-vs-case-insensitive is intentionally not pinned. The pin against longest-match reversal is deliberate: RFC 7484 §3 change would need a matching commit.
- **T7** — ✅ **DONE (with correction)** — The audit line placed the classifier in `dnsmod.detect_operators`; in reality it lives in `checks._is_large_anycast_operator` (and `LARGE_ANYCAST_ASNS`) — the classifier itself is unit-pinned by `tests/test_anycast_classifier.py` (8 tests). What was NOT pinned was the **call site** in `checks._nameservers` that emits the "Network diversity" finding — a refactor that kept the classifier intact but broke the wiring (e.g., passing `org` alone when `asn` is available, or reading the wrong `ip_rdap` field) would regress vergecloud.com back to a WARN without any classifier-level test noticing. New file `tests/test_operator_diversity_finding.py` (7 tests) monkeypatches the four DNS/IP-registry entry points (`get_ns_and_ips`, `parent_delegation`, `probe_each_ns`, `ip_rdap`) and drives `_nameservers` with synthetic host_info dicts, asserting: single-ASN anycast (AS141383) → INFO, single-ASN unknown → WARN, multi-ASN → PASS, brand-string fallback → INFO, unknown-brand fallback → WARN, mixed one-anycast-one-unknown → PASS (cardinality check), `skip_asn=True` suppresses the finding entirely. Load-bearing verified: forcing the classifier to always-False makes the vergecloud + brand-fallback tests red.
- **T8** — ✅ **DONE** — Added `.github/workflows/tests.yml`: triggers on `push` and `pull_request` (both targeting `main`), so branch protection can pin the offline job as a required status check and the workflow-run cache stays fresh on the main branch. Matrix: Python 3.10 (CP3 floor) and 3.12 on ubuntu-latest, plus one macOS 3.12 run to catch platform-specific breakage in dnspython / cryptography wheels. `concurrency` group cancels in-progress runs when a newer commit lands on the same ref. Installs via `pip install -e .[web,dev]` so the SPA test-only extra (httpx for TestClient, pytest-asyncio for SSE tests) is present. Runs `pytest -m "not network" -ra` — the T9 gate. Guarded by `tests/test_ci_workflow.py` (6 structural assertions: triggers on PR, triggers on push-to-main, uses the `not network` marker, tests Python 3.10, has a nightly schedule for the network suite, and pins action versions).
- **T9** — ✅ **DONE (rolled into T8 workflow)** — Same file now defines a separate `network` job that runs only on `schedule:` (03:17 UTC nightly) or `workflow_dispatch`, invoking `pytest -m "network" -ra`. Network-marked tests exist for cloudflare.com, google.com, dnssec-failed.org, and other ground-truth domains — their live-DNS flakiness would poison every PR if they gated merges, so they run out-of-band. A persistent nightly red is a signal that the tool's ground-truth expectations have drifted from real DNS state; investigate rather than dismiss. Pinned by the `nightly_network_suite_scheduled` test in `tests/test_ci_workflow.py`.

## Documentation Issues

- **DO1** — ✅ **DONE** — Rewrote `README.md` to catch up with the Phase-4/5 landscape. Added: preferred `pip install -e .[web]` path (with `[web,performance]` for opt-in uvloop on Linux/macOS), `posture-cli` and `posture-web` console-script names, a full CLI flag table (`--json`, `--no-info`, `--dkim-selector`, `--skip-asn`, `--strict`), Dockerfile invocation (`docker build` + `docker run -p 8000:8000`), self-test warnings banner (from L1), `pytest -m "not network"` as the CI contract with a note on why the `network` marker is nightly-only (flaky external DNS would produce false CI failures). Kept the legacy `run_cli.sh` / `run_web.sh` documentation as still-works fallbacks rather than removing them — the wrappers do useful things (venv guard from F2) and removing them would break external documentation pointing at them. Docs-only change; no test needed per CLAUDE.md rule 3 (rule 3 governs behavioural fixes, not documentation).
- **DO2** — ✅ **DONE** — Added `ROOT CAUSE PRINCIPLE (CLAUDE.md)` anchor comments at the five sites most prone to the "trust the shortcut" pattern, one per row of the CLAUDE.md table: (1) `checks._is_large_anycast_operator` — ASN before brand-string, the vergecloud/C4 lesson; (2) `dnsmod.parent_delegation` — parent zone's actual NS delegation before RDAP's NS list, the .bank.in/NIXI lesson; (3) `dnsmod.dnssec_status` — full DS→DNSKEY→chain-to-root before DNSKEY self-signature, the "self-signed but broken at parent" lesson; (4) `dnsmod.authoritative_vs_cached` — consensus + authoritative read before one recursive resolver's cache, with a cross-reference pointing at parent_delegation as the consensus leg; (5) `emailauth.TxtUnretrievable` — CLAUDE.md rule 1 (three states must never collapse) at the exception that carries "unretrievable" across the layer boundary. Plus a forward-pointer at `_registration`'s transfer-lock line naming EPP's full status code set as the source of truth (surfaced today only as `transferProhibited`, so this is a "widen when it becomes finding-relevant" note, not a bug). Docs-only change; no test needed per the DO1 precedent — the anchors ARE the fix, and behaviour is untouched (offline suite: 429 pass). A future author greppng for "ROOT CAUSE PRINCIPLE" finds every load-bearing shortcut/source-of-truth site in the codebase.
- **DO3** — ✅ **DONE (with correction)** — Audit line said "BUGS.md lists N1–N33" but the file actually numbers items T1–T6 (EPIC 0) + B1–B37 (P0–P3) — 43 items covering 33 distinct findings. Added a **"Cross-reference: status & pinning tests"** section at the bottom of `BUGS.md` — one row per ID naming current status (DONE / PARTIAL / WITHDRAWN / OPEN) and the pinning test file(s). Where a BUGS item was addressed via an AUDIT.md item under a different ID, both are named. Summary: 21 DONE, 8 PARTIAL, 2 WITHDRAWN, 12 OPEN. Reverse lookup is now a scan of one table rather than a grep across the tree. Docs-only change; no test needed per DO1 precedent. The alternative — an inline `**Status:**` line per item — was rejected as more invasive (would double file length) and less scannable than a consolidated table. A future author can navigate BUGS-ID → test file(s) in one hop.
- **DO4** — No architecture diagram. A one-page data-flow (CLI/Web → `checks.run` → sections → findings → grade) would meaningfully help onboarding.
- **DO5** — Grade model (correctness/hardening split, HARDENING_ABSENCE, FAIL floor) is undocumented outside code. Move the explanation to `docs/grading.md`.

## Technical Debt

- **TD1** — ✅ **RESOLVED (pointer to CP1 + CP2)** — Now installable via `pip install .` with `[project.scripts]` console entries `posture-cli` and `posture-web`. The shell wrappers still exist (`run_cli.sh`, `run_web.sh`) as a documented fallback for existing tutorials/docs, but the *primary* invocation is the console script and no longer depends on `.venv` state. `web.server:main()` embeds the `uvicorn.run(...)` call so it works on Windows without a POSIX shell. `pyproject.toml` declares `requires-python = ">=3.10"` (CP3) so `pip install` refuses on incompatible interpreters instead of silently installing and blowing up at import time. Nothing further to do at the TD1 layer — the audit item pointed at CP1/CP2 as the fix, and CP1/CP2 shipped.
- **TD2** — ✅ **NO CHANGE NEEDED — audit inaccurate** — The seven sections are already extracted as top-level functions: `_registration(rep, d)`, `_nameservers(rep, d, skip_asn=False)`, `_soa(rep, d, ns_map)`, `_records(rep, d)`, `_dnssec(rep, d)`, `_email(rep, d, dkim_selectors)`, `_security(rep, d)` — with `run()` and `run_streaming()` iterating a `section_steps` list of `(name, callable)` tuples. The 900-line file length reflects individual section complexity (SPF trace, DNSSEC state machine, MTA-STS policy fetch), not orchestrator inlining. Further extraction would move sections into their own modules — that's a `posture/sections/` package restructure, a distinct refactor from what TD2 as stated actually asks for. Recorded here rather than silently dropped so a future refactor doesn't reintroduce the audit item on the assumption that TD2 was left unaddressed.
- **TD3** — ✅ **RESOLVED (pointer to D1 + F4/P6 + C2 + C3)** — All four sub-claims are closed. `dnsmod._qcache` is an LRU-bounded `OrderedDict` (D1) with per-key TTL. `web/server.RESULT_CACHE` is an LRU-bounded `OrderedDict` (F4/P6) with per-domain result TTL. The "per-run memoisation absent for MTA-STS/DKIM" concern was actually about *duplicate calls within one run* — closed by C2 (single MTA-STS callsite in `checks._email`) and C3 (single `ex.map` pass in `evaluate_dkim`), each pinned by regression tests (`test_mta_sts.py::test_mta_sts_queries_the_sts_name_once`, `test_dkim_probe_count.py`). The two remaining caches serve different layers by design: `_qcache` is protocol-layer DNS caching with TTL semantics; `RESULT_CACHE` is application-layer completed-report caching with a fixed TTL. Collapsing them into one cache would erase the layer boundary and force the web layer to know about DNS record types. Recorded here rather than silently dropped so a future refactor doesn't reintroduce the audit item.
- **TD4** — ✅ **NO CHANGE NEEDED — audit conflates layers** — The two representations are a deliberate layering choice, not a divergence bug. `dnsmod.query` is the raw-wire layer with a single dict-based return contract shared by *every* record-type caller (A/AAAA/MX/CAA/TXT/NS/SOA/DNSKEY/DS — dozens of sites). `emailauth._txt_records` is the semantic normalisation layer specifically for TXT-heavy SPF/DKIM/DMARC callers who need raise-or-return semantics so they can propagate "unretrievable" cleanly through nested SPF `include:` chains. Collapsing the two would either (a) force every DNS caller to catch a new exception (invasive; regression risk across the whole codebase) or (b) turn `_txt_records` into a dict-returning function that dozens of email-auth callers would then need to inspect — trading raise-or-return propagation for scattered dict-key checks and re-introducing the exact "absent vs unretrievable" collapse the exception exists to prevent (CLAUDE.md rule 1). The AXFR path was extended for symmetry (D6) — TCP truncation raises `TxtUnretrievable` on the AXFR probe too — so the *pattern* IS standardised across the entire "protocol-level unretrievable" surface, just via mirror-implementations rather than a shared helper. Recorded here rather than silently dropped so a future refactor doesn't reintroduce the audit item as an "unresolved" task.
- **TD5** — No structured logging. `print`/`console.log` are used inconsistently.
- **TD6** — No metrics or tracing hooks; when the tool is public on vergecloud.com there is no way to see which checks are slow or failing at scale.

---

## Prioritization

**P0 — Fix immediately, blocks public exposure**
- C1 XSS in `app.js`

**P1 — Fix before Solutions Engineer hands this to a customer**
- C2 MTA-STS double-call
- C3 DKIM double-run
- C4 Hardcoded anycast brand list (mis-grades vergecloud.com)
- ~~C5 SPF mx undercount~~ **WITHDRAWN** — audit premise was incorrect; current code is RFC 7208 §4.6.4 compliant.
- S2 Wide-open CORS
- S3 X-Forwarded-For bypass
- FN4 Deprecated DNSSEC algorithms not flagged
- G1 Correctness/hardening sub-grades not surfaced

**P2 — Fix before first public release**
- D1 `_qcache` unbounded; F4 `RESULT_CACHE` unbounded
- D2 Single-resolver parent NS read
- D3–D6 DNS timeout/error-state cleanup
- S4 ReDoS in DOMAIN_RE
- S5–S7 Missing security headers, SSE timeout
- A1–A6 Accessibility
- W2–W5 UX issues
- CP1–CP7 Portability (all)
- G2, G3, G4 Grading model hardening + `google.com` regression test
- T1–T9 Test coverage build-out + CI

**P3 — Nice to have**
- S8, S9 UX/compliance banners, User-Agent
- L1 provisional flag surfacing polish
- W6 CSRF (only after CORS is tightened)
- E1–E7 Edge cases
- DO1–DO5 Documentation
- TD1–TD6 Technical debt

---

## Phased Implementation Plan

Each phase is a single mergeable increment. All phases assume the CLAUDE.md rule "**one bug, one test, one commit**".

### Phase 1 — Correctness (P0/P1 code bugs)
1. **C1 XSS** — whitelist `f.status`, escape in `innerHTML`, add JSDOM regression test. **DONE** (commit `6bf64a9`).
2. **C2 MTA-STS** — one lookup per invocation (bug was intra-function, not intra-orchestrator). **DONE** (commit `0818871`).
3. **C3 DKIM** — collapse to one `ex.map` pass; fixes shut-down-executor RuntimeError on live scans. **DONE** (commit `25a0d52`).
4. **C4 Anycast** — new `LARGE_ANYCAST_ASNS` dict as the primary classifier; brand-string set demoted to fallback; VergeCloud seeded. **DONE** (commit `b1a42cb`).
5. ~~**C5 SPF mx**~~ — **WITHDRAWN**. Audit premise misread RFC 7208 §4.6.4; current code is compliant. Comment fix + explicit RFC pin tests shipped.

### Phase 2 — DNS reliability
- D1 `_qcache` LRU cap; D2 dual-resolver parent NS read; D3 AXFR timeout config + UNKNOWN; D4 cryptography-missing message; D5 dual-source-subnet open resolver probe; D6 symmetric TxtUnretrievable for AXFR.

### Phase 3 — Grading
- G1 Print correctness/hardening sub-grades in CLI header and web UI.
- G2 Per-check hardening flag replacing global set.
- G3 `test_grading_google_unsigned.py` and companion fixtures for the four ground-truth domains.
- G4 Optional strict mode (env var `POSTURE_STRICT=1` promotes WARN→FAIL).

### Phase 4 — Portability
- CP1 `pyproject.toml` with `[project.scripts]` (`posture-cli`, `posture-web`).
- CP2 Pinned deps via `uv` or `pip-tools` lockfile.
- CP3 `requires-python = ">=3.10"`.
- CP4 `web` extra using plain uvicorn on Windows.
- CP5 Dockerfile + published image.
- CP6 `pathlib` sweep.
- CP7 Pin `cryptography` to a version with wheels for py310/311/312/313.

### Phase 5 — CLI/install polish
- Deprecate `run_cli.sh` and `run_web.sh` in favour of installed scripts.
- CLI `--strict`, `--json`, and `--no-info` documented in `README.md`.
- CLI header prints self-test state and provisional flag prominently.

### Phase 6 — Web UI / responsive / accessibility
- W2 SSE reconnect UI.
- W3 Mobile layout pass.
- W4 `prefers-color-scheme`.
- W5 self-test banner on landing.
- A1–A6 accessibility items (WCAG AA contrast, focus rings, aria-live, semantic markup, `<label>`, `lang`).

### Phase 7 — Security hardening
- S2 CORS → explicit allow-list (vergecloud.com only).
- S3 Rate-limit key by socket peer, not X-Forwarded-For (unless behind a trusted proxy; document).
- S4 Replace `DOMAIN_RE` with `idna` + explicit label checks.
- S5–S7 CSP, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, SSE per-connection timeout.
- S8 First-run "probing third-party infra" banner in CLI.
- S9 Explicit User-Agent on all outbound HTTP.

### Phase 8 — Performance
- P4 parallel per-NS probing.
- P5 RDAP bootstrap cache with TTL.
- P6 `RESULT_CACHE` LRU eviction.

### Phase 9 — CI/CD & observability
- T8 GitHub Actions matrix on Python 3.10–3.13 × Linux/macOS/Windows running `pytest -m "not network"`.
- T9 nightly job running `pytest -m network` against the four ground-truth domains.
- TD5 Replace `print` with `logging` module, structured JSON in web tier.
- TD6 Add OpenTelemetry hooks for future deployment (behind a `POSTURE_TELEMETRY_ENDPOINT` env var).

---

## Auditor's closing note

The tool is more coherent than its file count suggests: `checks.py` orchestrates cleanly, and the Root Cause Principle is genuinely reflected in how the DNS, RDAP, and DNSSEC modules are structured. The 33 previously-catalogued findings and the 22 that already have fixes plus the 59 regression tests added this session show a codebase moving in the right direction.

The audit's biggest concern is not any single bug, but that **the tool's own home domain (vergecloud.com) fails a check the tool itself is meant to authoritatively answer (C4)**. Until that is fixed, exposing the tool publicly is a marketing risk as well as a correctness one.

Second concern: the P0 XSS is one-line to fix but has been latent long enough that it must be treated as a lesson about the review process, not just a bug to close. Every `innerHTML` in the front-end should be re-audited in Phase 1.

No code changes were made. Awaiting explicit go-ahead before Phase 1.
